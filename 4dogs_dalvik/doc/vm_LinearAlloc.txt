[整体介绍]
LinearAlloc是线性内存分配器，存储简单的快速的不变的数据，比如不分类(虚函数表，字段，方法，接口),因为这些项目的生命周期绑定在类加载器上，需要一个与类加载器关联的存储结构存储这些数据.把数据存储在这里而不是系统堆上，减少堆的杂乱，并且加快类的加载，减少内存占用(减少堆结构的开销).重要的是，增加的页面数与启动的"Zygote mode"共享.

主要实现了对类加载器绑定的线性内存进行分配，重新分配，创建，更新页等操作，此线性内存貌似是分配在页的共享内存中.
